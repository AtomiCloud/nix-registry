---
name: nuget-package
description: Add or upgrade .NET/NuGet packages in the nix-registry
trigger: /nuget-package
---

# NuGet Package

Guide for working with .NET/NuGet packages (using fetchNuGet).

> **Note:** This project uses `pls` instead of `task` for running Taskfile commands.

See [Nuget Guide](../../docs/developer/packaging/Nuget.md) for complete documentation on:

- Adding new NuGet packages
- Upgrading existing packages
- fetchNuGet usage
- CI integration
