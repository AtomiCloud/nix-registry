---
name: bin-wrapper
description: Add or upgrade pre-built binary packages (binWrapper) in the nix-registry
trigger: /bin-wrapper
---

# Binary Wrapper Package

Guide for working with pre-built binary packages.

> **Note:** This project uses `pls` instead of `task` for running Taskfile commands.

See [BinWrapper Guide](../../docs/developer/packaging/BinWrapper.md) for complete documentation on:

- Adding new binWrapper packages
- Upgrading existing packages
- Platform-specific SHA256 handling
- CI integration
